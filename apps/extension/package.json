{
  "name": "@campusschedule/extension",
  "version": "0.1.0",
  "private": true,
  "description": "CampusSchedule Importer 浏览器扩展（Manifest V3）",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json && node build.mjs"
  },
  "dependencies": {
    "@campusschedule/adapters": "workspace:*",
    "@campusschedule/core": "workspace:*",
    "@campusschedule/importer-protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "esbuild": "^0.28.2"
  }
}
